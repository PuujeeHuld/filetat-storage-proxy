{
  "name": "filetat-storage-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "HMAC-token-validating download proxy in front of MinIO on an Oracle Cloud VM, matching filetat's signed-URL scheme (lib/storage/s3.ts).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
