PORT=8787

# Must be byte-for-byte the same value as this backend's `downloadTokenSecret`
# in filetat's backoffice (Файл хадгалалт → тухайн backend → Засах).
DOWNLOAD_TOKEN_SECRET=

# MinIO connection — same values entered as endpoint/region/accessKeyId/
# secretAccessKey/bucket when adding this backend in the backoffice.
MINIO_ENDPOINT=http://127.0.0.1:9000
MINIO_REGION=us-east-1
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_BUCKET=
